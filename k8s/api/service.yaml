apiVersion: v1
kind: Service
metadata:
  name: agilis-api
spec:
  selector:
    app: agilis-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
  type: NodePort
