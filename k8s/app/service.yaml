apiVersion: v1
kind: Service
metadata:
  name: typescript-api-starter
spec:
  selector:
    app: typescript-api-starter
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
  type: NodePort
